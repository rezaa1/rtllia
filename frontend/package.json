{"name": "retell-frontend",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0", // Update to latest version
    "react-dom": "^18.2.0", // Update to latest version
    "react-router-dom": "^6.10.0", // Update to latest version
    "axios": "^1.3.5", // Update to latest version
    "bootstrap": "^5.2.3", // Update to latest version
    "react-bootstrap": "^2.7.4" // Update to latest version
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}